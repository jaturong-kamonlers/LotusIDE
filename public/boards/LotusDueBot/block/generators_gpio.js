module.exports = function(Blockly){
  'use strict';
  // io_* generators are provided by the platform (arduino-sam/block/generators_gpio.js).
  // This file kept as a placeholder so KBIDE's loader does not complain about
  // a missing generator file paired with blocks_gpio.js.
};
